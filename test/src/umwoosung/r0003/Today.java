package umwoosung.r0003;
/*
dml delect insert update select
dll
dcl


static

class Exam{
static int a=3;

Exam e= new Exam();
s.o.p(e.a) <--3
ea.4 = 4
sop(e.a)<--4

1.new 로 생성하지 않아도된다.
2.스태틱으로 선언한 것들은 이텔릭글자로 보여진다.

metadate 어떤 데이터를 정의할수있는값.(컬럼값)
컬ㄹ럼 전체의 대한 컬럼값이 아니라 내가 정의한 컬럼값이 나오는 결과에 대한 데이터이다.
*/
